import DashboardCardTemplate from "./DashboardCardTemplate";

function RenderDashboardPlaylists({ title, details }) {
  return <DashboardCardTemplate title={title} details={details} />;
}

export default RenderDashboardPlaylists;
